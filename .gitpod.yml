image: gitpod/workspace-full

tasks:
  - command: | 
      curl -s https://get.nextflow.io | bash &&
      sudo mv nextflow /usr/local/bin/
      echo "Nextflow installed"

      chmod +x /workspace/NF_pipeline/nf-metatropics/bin/produce_fasta.pl
      chmod +x /workspace/NF_pipeline/nf-metatropics/bin/adding_bcfdepth_V2.pl
